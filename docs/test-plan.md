# Test plan
Run `npm test` for migration shape, foreign-key relationship integrity, tenant isolation, viewer denial, and operator escalation denial. Smoke test health, migration status, tenants, project listing, role update, and deletion with the sample IDs in `docs/fixtures/requests.md`.
