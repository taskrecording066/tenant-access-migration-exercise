# PR 3 — API enforcement
Scope: Express auth context, tenant middleware, endpoint behavior, migration status, and destructive action. Risk: middleware ordering and object-level checks. Review: test cross-tenant IDs and legacy identifiers. Test: full top-of-stack `npm test`, build, and curl smoke tests.
