# PR 2 — Authorization domain
Scope: roles, memberships, permission policy, repository access methods, and fixtures. Matrix is in `docs/authorization-matrix.md`. Risk: role semantics must never cross tenant boundaries. Review: challenge escalation and platform-admin behavior. Test: isolation and negative permission cases.
